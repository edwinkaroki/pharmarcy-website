# Python API port. The Vite frontend proxies /api requests here.
PORT=5000
# Optional: safe local verified responses are used when this is not set.
GEMINI_API_KEY=REPLACE_ME
GEMINI_MODEL=gemini-3.6-flash
PHARMACY_TIMEZONE=Africa/Nairobi
